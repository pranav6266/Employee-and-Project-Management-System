<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .card {
            border: none;
            border-radius: 0.75rem;
            box-shadow: 0 4px 12px rgba(0,0,0,.08);
        }
        .card-header {
            background-color: transparent;
            border-bottom: 1px solid #e9ecef;
            font-weight: 600;
            padding: 1rem 1.5rem;
        }
        .welcome-card {
            background: linear-gradient(to right, #007bff, #0056b3);
            color: white;
        }
        .table {
            border-collapse: separate;
            border-spacing: 0 0.75rem;
        }
        .table thead th {
            border: none;
            font-weight: 600;
            color: #6c757d;
        }
        .table tbody tr {
            background-color: #fff;
            box-shadow: 0 2px 4px rgba(0,0,0,.05);
            border-radius: 0.5rem;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .table tbody td {
            vertical-align: middle;
            padding: 1rem 1.5rem;
            border: none;
        }
        .table-hover tbody tr:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(0,0,0,.1);
        }
        .badge {
            padding: 0.5em 0.75em;
            font-size: 0.8rem;
        }
    </style>
</head>
<body>

<%- include('../partials/_user_navbar') %>

<div class="container mt-4">
    <div class="card text-center mb-4 welcome-card">
        <div class="card-body p-5">
            <h2 class="card-title">Welcome back, <%= user.name %>!</h2>
            <p class="card-text fs-5">You have <strong><%= assignedModules.length %></strong> pending or active tasks.</p>
            <a href="/users/projects" class="btn btn-light mt-2"><i class="bi bi-folder"></i> View All My Projects</a>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h4>My Assigned Tasks</h4>
        </div>
        <div class="card-body p-4">
            <% if (assignedModules.length > 0) { %>
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                        <tr>
                            <th>Task Title</th>
                            <th>Project</th>
                            <th>Status</th>
                            <th>Due Date</th>
                        </tr>
                        </thead>
                        <tbody>
                        <% assignedModules.forEach(module => { %>
                            <tr>
                                <td><%= module.title %></td>
                                <td><%= module.projectId.title %></td>
                                <td>
                                    <% if (module.status === 'Pending') { %>
                                        <span class="badge bg-warning text-dark"><%= module.status %></span>
                                    <% } else if (module.status === 'In Progress') { %>
                                        <span class="badge bg-info text-dark"><%= module.status %></span>
                                    <% } else { %>
                                        <span class="badge bg-success"><%= module.status %></span>
                                    <% } %>
                                </td>
                                <td><%= new Date(module.endDate).toLocaleDateString() %></td>
                            </tr>
                        <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="alert alert-info" role="alert">
                    You have no tasks assigned to you yet.
                </div>
            <% } %>
        </div>
    </div>
</div>

</body>
</html>