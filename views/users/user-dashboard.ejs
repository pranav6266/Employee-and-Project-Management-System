<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/users/dashboard">Dashboard</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                     <span class="navbar-text me-3">
                        Welcome, <strong><%= user.name %></strong>
                    </span>
                </li>
                <li class="nav-item">
                    <a href="/users/profile" class="nav-link">Profile</a>
                </li>
                <li class="nav-item">
                    <a href="/logout" class="btn btn-outline-light">Logout</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-4">
    <div class="card text-center mb-4">
        <div class="card-body">
            <h2 class="card-title">Welcome back, <%= user.name %>!</h2>
            <p class="card-text">Here is a summary of your assigned tasks. You have <strong><%= assignedModules.length %></strong> pending or active tasks.</p>
            <a href="/users/projects" class="btn btn-primary"><i class="bi bi-folder"></i> View All My Projects</a>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h4>My Assigned Tasks</h4>
        </div>
        <div class="card-body">
            <% if (assignedModules.length > 0) { %>
                <table class="table table-striped table-hover">
                    <thead>
                    <tr>
                        <th>Task Title</th>
                        <th>Project</th>
                        <th>Status</th>
                        <th>Due Date</th>
                    </tr>
                    </thead>
                    <tbody>
                    <% assignedModules.forEach(module => { %>
                        <tr>
                            <td><%= module.title %></td>
                            <td><%= module.projectId.title %></td>
                            <td>
                                <% if (module.status === 'Pending') { %>
                                    <span class="badge bg-warning text-dark"><%= module.status %></span>
                                <% } else if (module.status === 'In Progress') { %>
                                    <span class="badge bg-info text-dark"><%= module.status %></span>
                                <% } else { %>
                                    <span class="badge bg-success"><%= module.status %></span>
                                <% } %>
                            </td>
                            <td><%= new Date(module.endDate).toLocaleDateString() %></td>
                        </tr>
                    <% }); %>
                    </tbody>
                </table>
            <% } else { %>
                <div class="alert alert-info" role="alert">
                    You have no tasks assigned to you yet.
                </div>
            <% } %>
        </div>
    </div>
</div>

</body>
</html>