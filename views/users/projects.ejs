<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Projects</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .card {
            border: none;
            border-radius: 0.75rem;
            box-shadow: 0 4px 12px rgba(0,0,0,.08);
        }
        .card-body {
            padding: 2rem;
        }
        .table {
            border-collapse: separate;
            border-spacing: 0 0.75rem;
            margin-top: -0.75rem;
        }
        .table thead th {
            border: none;
            font-weight: 600;
            color: #6c757d;
        }
        .table tbody tr {
            background-color: #fff;
            box-shadow: 0 2px 4px rgba(0,0,0,.05);
            border-radius: 0.5rem;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .table tbody td {
            vertical-align: middle;
            padding: 1rem 1.5rem;
            border: none;
        }
        .table-hover tbody tr:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(0,0,0,.1);
        }
        .badge {
            padding: 0.5em 0.75em;
            font-size: 0.8rem;
        }
        .btn-primary {
            background-color: #0d6efd;
            border-color: #0d6efd;
        }
    </style>
</head>
<body>

<%- include('../partials/_user_navbar') %>

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h3>My Assigned Tasks</h3>
        <a href="/users/dashboard" class="btn btn-secondary"><i class="bi bi-arrow-left"></i> Back to Dashboard</a>
    </div>

    <div class="card">
        <div class="card-body">
            <% if (modules.length > 0) { %>
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead class="table-light">
                        <tr>
                            <th>Task Title</th>
                            <th>Parent Project</th>
                            <th>Status</th>
                            <th>Due Date</th>
                            <th>Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        <% modules.forEach(module => { %>
                            <tr>
                                <td><%= module.title %></td>
                                <td><%= module.projectId.title %></td>
                                <td>
                                    <% if (module.status === 'Pending') { %>
                                        <span class="badge bg-warning text-dark"><%= module.status %></span>
                                    <% } else if (module.status === 'In Progress') { %>
                                        <span class="badge bg-info text-dark"><%= module.status %></span>
                                    <% } else { %>
                                        <span class="badge bg-success"><%= module.status %></span>
                                    <% } %>
                                </td>
                                <td><%= new Date(module.endDate).toLocaleDateString() %></td>
                                <td>
                                    <a href="/users/project/<%= module._id %>/view-details" class="btn btn-sm btn-primary">
                                        <i class="bi bi-eye"></i> View Details
                                    </a>
                                </td>
                            </tr>
                        <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="alert alert-info">You have no tasks assigned to you.</div>
            <% } %>
        </div>
    </div>
</div>

</body>
</html>