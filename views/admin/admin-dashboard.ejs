<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/admin/dashboard">Admin Panel</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <span class="navbar-text me-3">
                        Welcome, <strong><%= user.name %></strong>
                    </span>
                </li>
                <li class="nav-item">
                    <a href="/logout" class="btn btn-outline-light">Logout</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-4">
    <div class="row">
        <div class="col-md-4">
            <div class="card text-white bg-primary mb-3">
                <div class="card-body text-center">
                    <i class="bi bi-people-fill fs-1"></i>
                    <h5 class="card-title mt-2">Total Employees</h5>
                    <p class="card-text fs-4"><%= totalEmployees %></p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card text-white bg-success mb-3">
                <div class="card-body text-center">
                    <i class="bi bi-briefcase-fill fs-1"></i>
                    <h5 class="card-title mt-2">Total Projects</h5>
                    <p class="card-text fs-4"><%= totalProjects %></p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card text-dark bg-light mb-3">
                <div class="card-body text-center">
                    <i class="bi bi-list-check fs-1"></i>
                    <h5 class="card-title mt-2">Project Statuses</h5>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Not Started
                            <span class="badge bg-secondary rounded-pill"><%= projectStatusCounts['Not Started'] || 0 %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            In Progress
                            <span class="badge bg-info rounded-pill"><%= projectStatusCounts['In Progress'] || 0 %></span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Completed
                            <span class="badge bg-success rounded-pill"><%= projectStatusCounts['Completed'] || 0 %></span>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h4>Management Actions</h4>
                </div>
                <div class="card-body">
                    <a href="/admin/employees" class="btn btn-lg btn-primary me-2">
                        <i class="bi bi-person-gear"></i> Manage Employees
                    </a>
                    <a href="/admin/projects" class="btn btn-lg btn-secondary">
                        <i class="bi bi-folder-symlink"></i> Manage Projects
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

</body>
</html>